<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Launch theme. Kept in XML (instead of setTheme() inside onCreate) so the window
        is already configured before the first frame is drawn - that removes the white
        flash between the launcher and the first Compose frame.
    -->
    <style name="Theme.ArrCenter" parent="android:Theme.DeviceDefault.Light.NoActionBar">
        <item name="android:windowBackground">?android:attr/colorBackground</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

</resources>
